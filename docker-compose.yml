services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: findart-api:latest
    container_name: findart-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "0.0.0.0:8000:8000"
